
## 一句话本质
> 类加载器在尝试自己加载一个类之前，**先逐级向上委托给父加载器**，直到最顶层的启动类加载器；父加载器能加载的就绝不自己加载，父加载器全部无法加载时才自己加载。

## 核心方法
`java.lang.ClassLoader#loadClass(String name, boolean resolve)`  
该方法用 `synchronized` 修饰，保证同一个类加载器实例的加载过程线程安全。

## 流程速记（三步骤）
1. **查缓存** —— `findLoadedClass(name)` 判断是否已加载，避免重复加载。
2. **委托父类加载器** —— 若未缓存：
   - `parent != null` → 调用 `parent.loadClass(name, false)`
   - `parent == null` → 调用 `findBootstrapClass0(name)`（启动类加载器）
3. **自己加载** —— 父加载器全部失败（抛 `ClassNotFoundException`），才调用自身的 `findClass(name)` 尝试加载。
4. （可选）若 `resolve == true` 则调用 `resolveClass(c)` 执行链接阶段的解析。

## 内存流程（伪代码）
```java
protected synchronized Class<?> loadClass(String name, boolean resolve) {
    // 1. 先查缓存
    Class<?> c = findLoadedClass(name);
    if (c == null) {
        try {
            // 2. 委派父类加载器（或启动类加载器）
            if (parent != null) {
                c = parent.loadClass(name, false);
            } else {
                c = findBootstrapClass0(name);
            }
        } catch (ClassNotFoundException e) {
            // 父加载器加载失败，继续往下
        }
        if (c == null) {
            // 3. 自己加载
            c = findClass(name);
        }
    }
    if (resolve) {
        resolveClass(c);
    }
    return c;
}